export class ProfileImage {
    name:string;
    url:string;
    mimeType:string;
    constructor(name:string, url:string, mimeType:string) {
        this.name = name;
        this.url = url;
        this.mimeType = mimeType;
    }
}
