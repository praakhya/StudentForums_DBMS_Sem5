package com.projects.pes.forumbackend.pojo;

import java.util.Objects;
//it is the json object representation of the data that its endpoint returns

public record Hello(String message, String name) {
}
